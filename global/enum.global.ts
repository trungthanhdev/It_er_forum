export enum Roles{
    USER = "USER",
    ADMIN = "ADMIN"
}

export enum HttpCode{
    SUCCESS = 200,
    ERROR = 404
}

export enum HttpMessage{
    SUCCESS = "SUCCESS",
    ERROR = "ERROR",
    INVALID_EMAIL = "INVALID_EMAIL"
}

